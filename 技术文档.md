# 技术文档

本文档记录当前 `auto_cad` 项目的业务逻辑、模块职责、函数接口、数据格式、运行流程和维护约定。文档基于当前代码状态整理，重点覆盖从 `Drawing1.dxf`、多数据表病害 Excel、隧道板长目录到最终批量 DXF 的自动化链路。

## 1. 项目目标

项目目标是自动生成带病害标注的衬砌平面图 DXF。

当前批量业务流程：

1. 从 `Drawing1.dxf` 中读取病害符号块。
2. 将病害符号块导出为 `batch_output\symbols.json`。
3. 从 `2025年鹤大隧道外观整理--本溪原始版本.xlsx` 中识别病害数据表。
4. 每个数据表只导出 C 列“项目名称”为 `衬砌` 的记录。
5. 每个数据表生成 `{数据表名称}_disease_report.json`。
6. 根据数据表名称匹配 `板长分布表` 目录中的对应隧道板长 Excel。
7. 每个数据表生成 `{数据表名称}_board_lengths.json`。
8. 根据符号、病害、板长数据生成 `{数据表名称}.dxf`。

旧的 `板长分布表.xlsx -> board_lengths.json -> lining_plan.dxf` 单文件默认链路已废弃并从 `main.cpp` 删除。当前无参数运行时，`main()` 会直接执行批量工作流。

## 2. 默认文件约定

项目根目录：

```text
D:\vs2022 code\auto_cad\
```

批量工作流默认输入文件：

```text
D:\vs2022 code\auto_cad\2025年鹤大隧道外观整理--本溪原始版本.xlsx
```

批量工作流默认板长来源目录：

```text
D:\vs2022 code\auto_cad\板长分布表\
```

批量工作流默认输出目录：

```text
D:\vs2022 code\auto_cad\batch_output\
```

批量工作流输出命名规则：

```text
batch_output\symbols.json
batch_output\{数据表名称}_disease_report.json
batch_output\{数据表名称}_board_lengths.json
batch_output\{数据表名称}.dxf
```

## 3. 总体模块划分

### 3.1 程序入口模块

文件：

```text
draw_cad/main.cpp
```

职责：

- 设置 Windows 控制台 UTF-8 编码，避免中文输出乱码。
- 定义默认输入输出路径。
- 提供命令行入口。
- 组装完整工作流。
- 调用各个业务模块，但不实现具体业务逻辑。

核心入口函数：

```cpp
int main(int argc, char* argv[]);
```

主要内部函数：

```cpp
void configureConsoleEncoding();
int runSymbolsJsonExport(const std::string& input_dxf, const std::string& output_json);
int runBoardLengthExport(const std::string& input_xlsx, const std::string& output_json);
int runDiseaseReportExport(const std::string& input_xlsx, const std::string& output_json);
int runLiningPlanDxfWrite(
    const std::string& disease_json,
    const std::string& board_lengths_json,
    const std::string& output_dxf,
    const std::string& symbols_json);
int runBatchWorkflow(
    const std::string& disease_xlsx,
    const std::string& board_lengths_source,
    const std::string& output_dir);
```

默认无参数流程：

```cpp
return runBatchWorkflow(
    defaultBatchDiseaseReportPath(),
    defaultBoardLengthDirectory(),
    defaultBatchOutputDir());
```

`runBatchWorkflow()` 调用顺序：

```text
runSymbolsJsonExport() -> batch_output\symbols.json
遍历病害 Excel 内所有病害数据表
runDiseaseReportExportForSheet() -> batch_output\{数据表名称}_disease_report.json
根据数据表名称匹配板长 Excel
runBoardLengthExport() -> batch_output\{数据表名称}_board_lengths.json
runLiningPlanDxfWrite() -> batch_output\{数据表名称}.dxf
```

病害数据表识别规则：

- 第 1 行 B-G 列表头必须依次包含 `衬砌板块号`、`项目名称`、`病害位置`、`检查内容`、`病害描述`、`判定结论`。
- 不符合该表头格式的统计表、评分表等工作表不会参与 DXF 生成。
- 数据表名称会作为输出文件名前缀；Windows 文件名非法字符会被替换为 `_`。

批量板长匹配规则：

- 板长来源默认是目录 `D:\vs2022 code\auto_cad\板长分布表\`。
- 目录内 Excel 文件名是对应隧道名称，例如 `新开岭隧道上行.xlsx`。
- 病害 Excel 数据表名通常不带 `隧道`，例如 `新开岭上行`。
- 匹配时会把板长文件名中的 `隧道` 去掉，再与病害数据表名比较。
- 每个数据表使用自己匹配到的板长文件，不再共用统一的 `board_lengths.json`。
- 如果找不到或找到多个匹配板长文件，批量流程会停止并输出错误，避免使用错误板长生成 DXF。

### 3.2 DXF 符号提取模块

文件：

```text
draw_cad/dxf_to_json.h
draw_cad/dxf_to_json.cpp
draw_cad/DxfSymbolCollector.h
draw_cad/DxfSymbolCollector.cpp
draw_cad/SymbolJsonWriter.h
draw_cad/SymbolJsonWriter.cpp
draw_cad/SymbolClassifier.h
draw_cad/SymbolClassifier.cpp
draw_cad/SymbolGeometry.h
draw_cad/SymbolGeometry.cpp
```

职责：

- 从 `Drawing1.dxf` 读取块定义。
- 只收集符合前缀规则的病害符号块。
- 将 DXF 图元统一转换为内部符号结构。
- 将符号写入 `symbols.json`。

默认块名前缀：

```cpp
std::string block_prefix = "SYM_";
```

核心类：

```cpp
class dxf_to_json final {
public:
    explicit dxf_to_json(MyConfig config);
    const std::vector<TuXing>& symbols() const noexcept;
    ConvertResult runExtractor();
};
```

配置结构：

```cpp
struct MyConfig {
    std::string input_dxf;
    std::string output_json;
    std::string unit = "m";
    std::string block_prefix = "SYM_";
};
```

结果结构：

```cpp
struct ConvertResult {
    int code;
    std::string message;
    bool ok() const noexcept;
    static ConvertResult success();
    static ConvertResult failure(int error_code, std::string error_message);
};
```

内部符号结构：

```cpp
struct Point2D {
    double x;
    double y;
};

struct BBox {
    double min_x;
    double min_y;
    double max_x;
    double max_y;
};

struct TuYuanStyle {
    std::string layer;
    std::string linetype;
    double lineweight_mm;
};

struct LineTuYuan {
    Point2D a;
    Point2D b;
};

struct PolylineTuYuan {
    std::vector<Point2D> points;
    bool closed;
};

struct TuYuan {
    TuYuanGeometry geometry;
    TuYuanStyle style;

    static TuYuan makeLine(const Point2D& a, const Point2D& b, const TuYuanStyle& style);
    static TuYuan makePolyline(std::vector<Point2D> points, bool closed, const TuYuanStyle& style);
};

struct TuXing {
    std::string block_name;
    Point2D anchor_point;
    std::vector<TuYuan> tuyuan_vector;
    StyleUnify style_unify;
};
```

### 3.3 符号分类和几何处理

`SymbolClassifier` 根据块名推断符号类型和缩放模式。

接口：

```cpp
class SymbolClassifier final {
public:
    static std::string BlockName_to_Kind(const std::string& block_name);
    static std::string inferScaleMode(const std::string& kind);
};
```

常见符号 ID：

```text
crack_long        纵向裂缝
crack_trans       环向裂缝 / 横向裂缝
crack_diag        斜向裂缝
crack_alligator   网状裂缝 / 龟裂
seepage           渗水印迹
rebar             露筋
damage            破损 / 涂料脱落 / 涂层脱落
```

`SymbolGeometry` 处理弧线、圆、多段线 bulge、归一化、包围盒等几何计算。

接口：

```cpp
class SymbolGeometry {
public:
    static std::vector<Point2D> discretizeArc(
        const Point2D& center,
        double radius,
        double start_rad,
        double end_rad);

    static std::vector<Point2D> discretizeCircle(const Point2D& center, double radius);

    static void appendBulgedSegment(
        std::vector<Point2D>& out,
        const Point2D& p0,
        const Point2D& p1,
        double bulge);

    static bool nearlyEqual(const Point2D& a, const Point2D& b, double eps = 1e-9);
    static void normalizeByAnchor(TuXing& tuxing);
    static BBox computeBBox(const TuXing& tuxing);
};
```

### 3.4 Excel 通用读取模块

文件：

```text
draw_cad/InspectionRow.h
draw_cad/InspectionTableReader.h
draw_cad/InspectionTableReader.cpp
```

职责：

- 读取 `.xlsx` 工作表。
- 按表头名称定位列。
- 将业务列封装为 `InspectionRow`。

数据结构：

```cpp
struct InspectionRow {
    std::string stake_no;
    std::string slab_no_raw;
    std::string project_name;
    std::string defect_location_raw;
    std::string check_item_raw;
    std::string defect_desc_raw;
    std::string judgement_raw;
    std::string inspect_date_raw;
    int source_row = 0;
};
```

读取类：

```cpp
class InspectionTableReader final {
public:
    struct ColumnMapping {
        std::string stake_no;
        std::string slab_no;
        std::string project_name;
        std::string defect_location;
        std::string check_item;
        std::string defect_desc;
        std::string judgement;
        std::string inspect_date;
    };

    struct Options {
        std::string sheet_name;
        uint32_t header_row = 1;
        bool skip_empty_rows = true;
        bool trim_text = true;
        ColumnMapping columns;
    };

    struct ReadResult {
        std::vector<InspectionRow> rows;
        std::vector<std::string> warnings;
        std::vector<std::string> errors;
        bool ok() const;
    };

    explicit InspectionTableReader(Options options = {});
    ReadResult read(const std::string& file_path) const;
};
```

说明：

- 当前病害报告导出模块仍使用固定列读取业务字段。
- `InspectionTableReader` 是独立通用读取模块，可用于后续把固定列逻辑迁移为表头匹配逻辑。

### 3.5 病害报告 JSON 导出模块

文件：

```text
draw_cad/DiseaseReportJsonExporter.h
draw_cad/DiseaseReportJsonExporter.cpp
```

职责：

- 从 `病害检测报告.xlsx` 读取病害记录。
- 解析病害描述中的距离、长度、宽度、面积。
- 保留 G 列“判定结论”为 0 的病害。
- 输出 `disease_report.json`。

接口：

```cpp
class DiseaseReportJsonExporter final {
public:
    struct Options {
        std::string sheet_name;
        uint32_t header_row = 1;
        bool skip_empty_rows = true;
        std::string project_name_filter;
    };

    struct ExportResult {
        int code = 0;
        std::string message;
        size_t exported_count = 0;
        size_t zero_judgement_count = 0;
        std::vector<std::string> warnings;
        bool ok() const noexcept;
        static ExportResult success(
            size_t exported,
            size_t zero_judgement,
            std::vector<std::string> warnings = {});
        static ExportResult failure(int error_code, std::string error_message);
    };

    static ExportResult run(
        const std::string& input_xlsx,
        const std::string& output_json,
        const Options& options = {});
};
```

默认列位：

```text
B列  衬砌板块号
C列  项目名称
D列  病害位置
E列  检查内容
F列  病害描述
G列  判定结论
```

项目名称过滤规则：

- 默认 `project_name_filter` 为 `衬砌`。
- C 列不是 `衬砌` 的记录会被跳过，不写入 `disease_report.json`。
- 若后续确实需要导出非衬砌项目，可在调用 `DiseaseReportJsonExporter::run()` 时显式传入其它 `project_name_filter`。

输出 JSON 结构：

```json
{
  "source": {
    "file": "...",
    "sheet": "...",
    "header_row": 1
  },
  "summary": {
    "exported_count": 48,
    "project_name_filter": "衬砌",
    "skipped_project_count": 17,
    "zero_judgement_count": 29
  },
  "records": [
    {
      "source_row": 37,
      "slab_no": "43",
      "project_name": "衬砌",
      "check_item": "纵向裂缝",
      "defect_location": "右拱腰",
      "defect_desc": "...",
      "judgement": "0",
      "judgement_is_zero": true,
      "parsed": {
        "distance_from_slab_end": "0m",
        "length": "9m",
        "width": "0.1mm",
        "area": ""
      }
    }
  ]
}
```

业务规则：

- `judgement_is_zero = true` 的记录仍写入 JSON。
- 只有 C 列“项目名称”为 `衬砌` 的记录写入 JSON。
- `summary.skipped_project_count` 记录因项目名称不匹配而跳过的行数。
- `parsed.distance_from_slab_end` 来自病害描述里的距板端信息。
- `parsed.length`、`parsed.width`、`parsed.area` 分别用于后续符号缩放和标注。

### 3.6 板长 JSON 导出模块

文件：

```text
draw_cad/BoardLengthJsonExporter.h
draw_cad/BoardLengthJsonExporter.cpp
```

职责：

- 从 `板长分布表` 目录中的单个隧道板长 Excel 读取板号和板长。
- 支持左右两组列。
- 输出 `{数据表名称}_board_lengths.json`。

接口：

```cpp
class BoardLengthJsonExporter final {
public:
    struct Options {
        std::string sheet_name;
        uint32_t header_row = 2;
        bool skip_empty_rows = true;
    };

    struct ExportResult {
        int code = 0;
        std::string message;
        size_t exported_count = 0;
        std::vector<std::string> warnings;
        bool ok() const noexcept;
        static ExportResult success(size_t exported, std::vector<std::string> warnings = {});
        static ExportResult failure(int error_code, std::string error_message);
    };

    static ExportResult run(
        const std::string& input_xlsx,
        const std::string& output_json,
        const Options& options = {});
};
```

默认列位：

```text
A列  slab_no
B列  length_m
D列  slab_no
E列  length_m
```

输出 JSON 结构：

```json
{
  "source": {
    "file": "...",
    "sheet": "...",
    "header_row": 2,
    "columns": {
      "A": "slab_no",
      "B": "length_m",
      "D": "slab_no",
      "E": "length_m"
    }
  },
  "summary": {
    "exported_count": 118
  },
  "records": [
    {
      "source_row": 3,
      "group": "A:B",
      "slab_no": "1",
      "length": "8m",
      "length_raw": "8",
      "length_m": 8.0
    }
  ]
}
```

校验规则：

- 有板号但没有长度，跳过并记录 warning。
- 有长度但没有板号，跳过并记录 warning。
- 长度不是严格数字，跳过并记录 warning。
- 重复板号会记录 warning。
- 成功数据按板号排序。

### 3.7 衬砌平面图 DXF 输出模块

文件：

```text
draw_cad/LiningPlanDxfWriter.h
draw_cad/LiningPlanDxfWriter.cpp
```

职责：

- 读取 `{数据表名称}_disease_report.json` 中的唯一板号。
- 读取 `{数据表名称}_board_lengths.json` 中的板长。
- 读取 `symbols.json` 中的病害符号。
- 生成衬砌平面图和病害标注。
- 输出 `{数据表名称}.dxf`。

接口：

```cpp
class LiningPlanDxfWriter final {
public:
    struct Options {
        size_t slabs_per_panel = 5;
        double drawing_units_per_meter = 1.0;
        double default_slab_length_m = 8.0;
        double label_column_width = 10.0;
        double zone_height = 2.0;
        double bottom_label_height = 0.75;
        double panel_gap_y = 2.0;
        double text_height = 0.35;
        double slab_no_text_height = 0.4;
        std::string symbols_json;
        double defect_symbol_scale = 0.22;
        double annotation_text_height = 0.32;
        double annotation_line_spacing = 0.42;
        double annotation_gap = 0.30;
        double disease_collision_padding = 0.12;
        double disease_collision_step_x = 0.45;
        double disease_collision_step_y = 0.45;
        size_t disease_collision_max_offset_steps = 4;
        double disease_horizontal_line_clearance = 0.20;
        double break_line_gap = 0.18;
        double break_offset_x = 0.35;
        double break_half_height = 0.55;
    };

    struct WriteResult {
        int code = 0;
        std::string message;
        size_t slab_count = 0;
        size_t panel_count = 0;
        std::vector<std::string> warnings;
        bool ok() const noexcept;
        static WriteResult success(size_t slabs, size_t panels, std::vector<std::string> warnings = {});
        static WriteResult failure(int error_code, std::string error_message);
    };

    static WriteResult run(
        const std::string& disease_report_json,
        const std::string& board_lengths_json,
        const std::string& output_dxf,
        const Options& options = {});
};
```

### 3.8 Warning/Error 日志模块

文件：

```text
draw_cad/WorkflowMessageLog.h
draw_cad/WorkflowMessageLog.cpp
```

职责：

- 统一显示业务流程中的 warning 和 error。
- 将 warning/error 同步写入 UTF-8 BOM 编码的 txt 文件。
- 批量流程默认输出到 `batch_output\warnings_errors.txt`。
- 单独命令默认输出到项目根目录 `warnings_errors.txt`。

接口：

```cpp
class WorkflowMessageLog final {
public:
    enum class Level {
        Warning,
        Error
    };

    WorkflowMessageLog() = default;
    explicit WorkflowMessageLog(const std::string& output_txt);

    bool open(const std::string& output_txt, std::string* error_message = nullptr);
    const std::string& path() const noexcept;

    void warning(const std::string& message);
    void error(const std::string& message);
};
```

输出格式：

```text
[WARNING] Row 39 group A:B has a non-numeric length and was skipped: 人行横洞
[ERROR] Failed to resolve board length workbook for sheet ...
```

## 4. 衬砌平面图绘制规则

### 4.1 板块分组

默认每块平面图显示 5 个衬砌板号：

```cpp
slabs_per_panel = 5;
```

板号来源：

```text
disease_report.json.records[*].slab_no
```

补充规则：

- 常规板号仍来自 `disease_report.json.records[*].slab_no`。
- 对于带长度的纵向病害，绘图前会结合起始板号、距板端距离、病害长度和 `board_lengths.json` 自动计算影响范围。
- 如果纵向病害跨到后续板号，即使后续板号没有独立病害记录，也会自动补入衬砌平面图。
- 目前跨板补齐只支持可解析为数字的板号；非数字板号仍按原单板逻辑处理。

板长来源：

```text
board_lengths.json.records[*].length_m
```

缺失板长时使用：

```cpp
default_slab_length_m = 8.0;
```

### 4.2 主体区域

当前衬砌主体为 4 个方框，从上到下为：

```text
左侧边墙
左侧拱腰
右侧拱腰
右侧边墙
```

左侧显示 5 条线名：

```text
左侧边墙底线
左侧拱腰线
拱顶线
右侧拱腰线
右侧边墙底线
```

其中 `拱顶线` 是中间横线，是上方两个区域和下方两个区域的分界线。

### 4.3 板号分割线

相邻板号：

```text
右侧板号 = 左侧板号 + 1
```

使用直线分割。

不相邻板号：

```text
右侧板号 != 左侧板号 + 1
```

使用双折线断开符号，折线有两个方向相反的角。

### 4.4 中文 DXF 输出

DXF 文字样式：

```cpp
CN_TEXT
simsun.ttc
```

中文文本会转换为 DXF Unicode 转义：

```text
\U+XXXX
```

这样可减少 CAD 软件中中文不显示或乱码的问题。

## 5. 病害绘制规则

### 5.1 病害符号映射优先级

映射以 `check_item` 为主，`defect_desc` 只做兜底。

规则：

```text
先识别 check_item
如果 check_item 已匹配符号，直接使用该符号
如果 check_item 无法识别，再使用 defect_desc 兜底
```

示例：

```text
check_item = 纵向裂缝
defect_desc 包含 伴有渗水印迹
最终符号 = crack_long
```

这样可以避免主病害为裂缝时，被描述中的伴随渗水误映射为渗水印迹符号。

### 5.2 符号缩放

缩放规则：

```text
纵向裂缝：按 length 横向缩放
环向裂缝 / 横向裂缝：按 length 纵向缩放
斜向裂缝：不按 length 缩放，使用默认符号比例
其它病害：使用默认符号比例
```

纵向跨板补充规则：

- 纵向裂缝、露筋等沿隧道方向缩放的病害，会先按板长计算影响到的后续板号。
- 同一绘图面板内，相同 `source_row` 的连续纵向段会合并成一条连续病害线，避免 022 和 023 被画成两条裂缝。
- 示例：022 板长 8m，病害距板端 6m，长度 5m，则输出为一条从 022 延伸到 023、图面长度为 5m 的连续裂缝。
- 如果跨越到下一绘图面板，才会按面板拆成多条绘制段；首段保留原始 `L=...`、`W=...` 等文字标注，后续面板段不重复标注。
- 连续绘制段仍使用原有位置带、避让和符号绘制逻辑。

说明：

- `斜向裂缝` 仍可以显示 `L=...` 文字。
- `斜向裂缝` 的图形尺寸不随 `length` 改变。

### 5.3 病害位置映射

位置字段：

```text
disease_report.json.records[*].defect_location
```

主要位置：

```text
左侧边墙
左拱腰
拱顶
右拱腰
右侧边墙
```

逻辑：

- `左拱腰` 病害只能在左拱腰区域内避让。
- `右拱腰` 病害只能在右拱腰区域内避让。
- 纯 `拱顶` 病害限制在拱顶线 `-0.2m` 到 `+0.2m` 范围内。
- `拱顶右拱腰` 这类组合位置优先按具体拱腰区域处理，避免误按纯拱顶处理。

### 5.4 病害重叠避让

每个病害生成占用矩形。

占用矩形包含：

```text
病害符号
渗水印迹内部面积文字
普通病害旁边文字标注
```

避让策略：

```text
优先原始位置
尝试上下偏移
尝试左右偏移
计算与已放置病害的重叠面积
计算越出所属位置带的惩罚
选择重叠面积最小的位置
```

配置参数：

```cpp
disease_collision_padding = 0.12;
disease_collision_step_x = 0.45;
disease_collision_step_y = 0.45;
disease_collision_max_offset_steps = 4;
disease_horizontal_line_clearance = 0.20;
```

### 5.5 横向分隔线避让

病害图形应放在方框内部，不应与横向分隔线重合。

规则：

- 病害图形避开横向分隔线。
- 文字描述允许压在横向线上。
- 避让横线时只约束病害图形本身，不强制移动文字。

### 5.6 标注文字

渗水印迹：

```text
面积文字放在符号内部
```

其它病害：

```text
在符号旁边显示文字描述
```

常见文字：

```text
L=9m
A=0.4m²
W=0.1mm
已修补
封缝料起皮
修补处起皮
伴随渗水印迹
```

## 6. JSON 文件格式

### 6.1 symbols.json

来源：

```text
Drawing1.dxf
```

生成模块：

```cpp
dxf_to_json
SymbolJsonWriter
```

结构示例：

```json
{
  "meta": {
    "unit": "m",
    "version": 1
  },
  "symbols": [
    {
      "id": "crack_long",
      "block": "SYM_CRACK_LONG",
      "kind": "LongitudinalCrack",
      "anchor": [0, 0],
      "bbox": {
        "min": [0, 0],
        "max": [9.902524, 0.026821]
      },
      "style": {
        "layer": "0",
        "linetype": "BYLAYER",
        "lineweight_mm": 0.29,
        "mixed": false
      },
      "params": {
        "scale_mode": "stretch_x",
        "base_length_m": 9.902524
      },
      "primitives": []
    }
  ]
}
```

### 6.2 board_lengths.json

来源：

```text
板长分布表\{隧道名称}.xlsx
```

用途：

```text
控制每个衬砌板号在 DXF 中的横向宽度
```

核心字段：

```text
slab_no
length_m
```

### 6.3 disease_report.json

来源：

```text
病害检测报告.xlsx
```

用途：

```text
确定需要绘制的板号、病害类型、位置、尺寸和标注
```

核心字段：

```text
slab_no
project_name
check_item
defect_location
defect_desc
judgement
judgement_is_zero
parsed.distance_from_slab_end
parsed.length
parsed.width
parsed.area
```

## 7. 命令行入口

可执行文件：

```powershell
D:\vs2022 code\auto_cad\x64\Debug\draw_cad.exe
```

### 7.1 默认批量工作流

无参数运行：

```powershell
& 'D:\vs2022 code\auto_cad\x64\Debug\draw_cad.exe'
```

无参数运行等价于：

```powershell
& 'D:\vs2022 code\auto_cad\x64\Debug\draw_cad.exe' --batch-workflow
```

### 7.2 单独导出符号 JSON

```powershell
& 'D:\vs2022 code\auto_cad\x64\Debug\draw_cad.exe' --symbols 'D:\vs2022 code\auto_cad\Drawing1.dxf' 'D:\vs2022 code\auto_cad\symbols.json'
```

### 7.3 单独导出板长 JSON

```powershell
& 'D:\vs2022 code\auto_cad\x64\Debug\draw_cad.exe' --board-lengths 'D:\vs2022 code\auto_cad\板长分布表\新开岭隧道上行.xlsx' 'D:\vs2022 code\auto_cad\batch_output\新开岭上行_board_lengths.json'
```

### 7.4 单独导出病害报告 JSON

```powershell
& 'D:\vs2022 code\auto_cad\x64\Debug\draw_cad.exe' --disease-report 'D:\vs2022 code\auto_cad\2025年鹤大隧道外观整理--本溪原始版本.xlsx' 'D:\vs2022 code\auto_cad\batch_output\新开岭上行_disease_report.json'
```

### 7.5 单独生成衬砌平面图 DXF

```powershell
& 'D:\vs2022 code\auto_cad\x64\Debug\draw_cad.exe' --lining-plan 'D:\vs2022 code\auto_cad\batch_output\新开岭上行_disease_report.json' 'D:\vs2022 code\auto_cad\batch_output\新开岭上行_board_lengths.json' 'D:\vs2022 code\auto_cad\batch_output\新开岭上行.dxf' 'D:\vs2022 code\auto_cad\batch_output\symbols.json'
```

### 7.6 批量生成多个数据表的 DXF

无参数批量运行：

```powershell
& 'D:\vs2022 code\auto_cad\x64\Debug\draw_cad.exe' --batch-workflow
```

显式指定输入和输出目录：

```powershell
& 'D:\vs2022 code\auto_cad\x64\Debug\draw_cad.exe' --batch-workflow 'D:\vs2022 code\auto_cad\2025年鹤大隧道外观整理--本溪原始版本.xlsx' 'D:\vs2022 code\auto_cad\板长分布表' 'D:\vs2022 code\auto_cad\batch_output'
```

执行规则：

- 程序先生成批量目录下的 `symbols.json`。
- 每个符合病害报告表头的数据表会单独生成 `{数据表名称}_disease_report.json`。
- 每个存在衬砌病害记录的数据表会按表名匹配一个板长 Excel，并单独生成 `{数据表名称}_board_lengths.json`。
- 每个存在衬砌病害记录的数据表会单独生成 `{数据表名称}.dxf`。
- C 列不是 `衬砌` 的记录不会进入中间 JSON，也不会进入 DXF。

## 8. 构建方式

推荐构建命令：

```powershell
$pathValue = $env:Path
[Environment]::SetEnvironmentVariable('PATH', $null, 'Process')
[Environment]::SetEnvironmentVariable('Path', $pathValue, 'Process')
& 'D:\vs2022\Microsoft Visual Studio\2022\Community\MSBuild\Current\Bin\MSBuild.exe' 'draw_cad\draw_cad.vcxproj' /t:Build /p:Configuration=Debug /p:Platform=x64 /p:OutDir='D:\vs2022 code\auto_cad\x64\Debug' /v:minimal
```

说明：

- 当前环境可能同时存在 `Path` 和 `PATH`。
- MSBuild 调用编译器时可能因环境变量重复报错。
- 上述命令会先规范当前 PowerShell 进程的 `Path`。
- 当前 `.sln` 中包含一个不可用的 `auto_cad_gui` 项目时，建议直接构建 `draw_cad.vcxproj`。

## 9. 当前验证结果

最近一次完整流程运行结果：

```text
symbols(raw): 7
board_lengths exported rows: 118
disease_report exported rows: 48
zero judgement rows: 29
lining_plan slabs: 36
lining_plan panels: 8
```

最近一次批量流程运行结果：

```text
disease workbook: D:\vs2022 code\auto_cad\2025年鹤大隧道外观整理--本溪原始版本.xlsx
board length source: D:\vs2022 code\auto_cad\板长分布表
output dir: D:\vs2022 code\auto_cad\batch_output
disease worksheets: 30
generated dxf files: 30
skipped sheets: 0
generated per-sheet board length JSON files: 30
```

控制台中文输出已设置 UTF-8：

```cpp
SetConsoleOutputCP(CP_UTF8);
SetConsoleCP(CP_UTF8);
std::setlocale(LC_ALL, ".UTF-8");
```

## 10. 常见 warning

板长导出 warning 示例：

```text
Row 62 group A:B has a non-numeric length and was skipped: 人行横洞
Row 62 group D:E has slab number but no length; skipped.
Row 63 group A:B has a non-numeric length and was skipped: 车行横洞
Row 63 group D:E has a non-numeric length and was skipped: 车行横洞
```

含义：

- Excel 中存在非普通板号的横洞信息。
- 当前板长模块只导出正常板号和数值板长。
- 这些 warning 不阻断最终 DXF 生成。

链接 warning 示例：

```text
OpenXLSXd.lib(...) : warning LNK4099: 未找到 PDB
```

含义：

- 第三方库缺少调试符号文件。
- 不影响程序运行和输出文件生成。

## 11. 错误码概览

### 11.1 dxf_to_json

```text
1  读取 DXF 文件失败
2  打开 symbols.json 输出文件失败
3  写 symbols.json 失败
```

### 11.2 DiseaseReportJsonExporter

```text
32 output json 路径为空
38 打开输出 JSON 失败
39 写输出 JSON 失败
40 导出过程中发生异常
```

### 11.3 BoardLengthJsonExporter

```text
52 output json 路径为空
58 打开输出 JSON 失败
59 写输出 JSON 失败
60 导出过程中发生异常
```

### 11.4 LiningPlanDxfWriter

```text
71 disease report json 路径为空
72 board lengths json 路径为空
73 output dxf 路径为空
74 slabs_per_panel 非法
75 drawing_units_per_meter 非法
76 default_slab_length_m 非法
77 disease_report.json 中没有板号
78 写 DXF 失败
79 生成 DXF 过程中发生异常
80 defect_symbol_scale 非法
81 annotation_text_height 非法
82 symbols.json 无有效符号
83 disease_collision_padding 非法
84 disease_collision_step_x 非法
85 disease_collision_step_y 非法
86 disease_horizontal_line_clearance 非法
```

## 12. 维护约定

### 12.1 不要混淆主病害和伴随描述

病害符号映射必须优先使用 `check_item`。

例如：

```text
check_item = 纵向裂缝
defect_desc = ...伴有渗水印迹...
```

符号应为：

```text
crack_long
```

不应为：

```text
seepage
```

### 12.2 拱顶位置需要特殊控制

纯 `拱顶` 病害应限制在：

```text
拱顶线 -0.2m 到 拱顶线 +0.2m
```

组合位置如 `拱顶右拱腰` 不按纯拱顶处理，应优先落到对应拱腰区域。

### 12.3 斜向裂缝不缩放

`斜向裂缝` 只显示长度文字，不根据 `length` 改变图形大小。

### 12.4 病害图形和文字避让规则不同

病害图形：

```text
必须尽量放在方框内
必须避开横向分隔线
```

文字描述：

```text
允许压在横向分隔线上
```

### 12.5 位置名称同义词必须显式映射

病害报告中的位置名称可能存在同义写法，不能只识别图纸标签中的完整名称。

已确认并修复的案例：

```text
数据表：鼻子沟上行
source_row：7
原始位置：左侧墙
问题表现：被通用“侧墙”规则映射到右侧边墙底部区域
修复位置：draw_cad/LiningPlanDxfWriter.cpp::diseaseLocationBand()
修复内容：将“左侧墙”显式映射到顶部“左侧边墙”区域
验证结果：鼻子沟上行.dxf 中第一处 L=2m 的文字 Y 坐标从 2.51083262 修正为 8.07
```

当前边墙映射规则：

```text
左侧边墙 / 左边墙 / 左侧墙 -> 左侧边墙区域
右侧边墙 / 右边墙 / 右侧墙 -> 右侧边墙区域
仅出现“侧墙”且无左右信息 -> 默认右侧边墙区域
```

## 13. 后续建议

可维护方向：

1. 将 `DiseaseReportJsonExporter` 的固定列读取改为复用 `InspectionTableReader` 的表头匹配能力。
2. 将 `LiningPlanDxfWriter::Options` 暴露为配置文件或命令行参数。
3. 为 `symbolIdForDisease()` 增加单元测试，锁定病害类型映射优先级。
4. 为拱顶、左拱腰、右拱腰、边墙位置映射增加单元测试。
5. 增加 DXF 输出快照测试，防止折线断开符号、病害缩放、中文文本样式回归。
