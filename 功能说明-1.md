# 功能说明-1

> 历史快照说明：当前维护中的拆分版文档入口为 `技术文档/README.md`。本文件保留早期 CAD 功能说明，作为历史参考；若与 `技术文档/` 目录中的文档冲突，以 `技术文档/` 为准。

本文档说明本窗口新增和调整的业务功能代码，覆盖 Excel 数据读取、病害报告 JSON 导出、板长 JSON 导出、衬砌平面图 DXF 输出，以及 DXF 中文和非连续板号折线样式修正。

## 1. 功能总览

本次新增功能围绕“从 Excel/JSON 数据生成衬砌平面图 DXF”形成一条可独立运行的数据链路：

1. 从病害检测报告 Excel 读取指定业务列。
2. 将病害检测报告导出为 `disease_report.json`。
3. 将板长分布表导出为 `board_lengths.json`。
4. 读取两个 JSON，按病害记录中的 `slab_no` 生成衬砌平面图 DXF。
5. 在 DXF 中按板号是否连续决定分割线样式：
   - 相邻板号：直线分割。
   - 不相邻板号：折线断开符号分割。
6. 每块图最多显示 5 个衬砌板号。
7. 每个衬砌的横向宽度来自 `board_lengths.json` 的 `length_m`。
8. DXF 左侧中文标签使用兼容 CAD 的 Unicode 转义和中文文字样式，避免中文不显示。

## 2. 新增和修改的主要文件

### 2.1 Excel 通用读取模块

文件：

- `draw_cad/InspectionRow.h`
- `draw_cad/InspectionTableReader.h`
- `draw_cad/InspectionTableReader.cpp`

职责：

- 从 `.xlsx` 工作表中按表头名称定位业务列。
- 读取用户需要的列内容，并封装成 `InspectionRow`。
- 支持指定工作表、表头行、是否跳过空行、是否裁剪文本空白。
- 对 Excel 日期单元格做字符串格式化。

### 2.2 病害报告 JSON 导出模块

文件：

- `draw_cad/DiseaseReportJsonExporter.h`
- `draw_cad/DiseaseReportJsonExporter.cpp`

职责：

- 从病害检测报告 Excel 中导出全部病害记录。
- 生成 `disease_report.json`。
- 从病害描述中解析距离板端、长度、宽度、面积等字段。
- 保留 G 列“判定结论”为 0 的记录，并标记 `judgement_is_zero`。

### 2.3 板长 JSON 导出模块

文件：

- `draw_cad/BoardLengthJsonExporter.h`
- `draw_cad/BoardLengthJsonExporter.cpp`

职责：

- 从板长分布表 Excel 导出每个衬砌板号的长度。
- 生成 `board_lengths.json`。
- 支持读取两组列：`A:B` 和 `D:E`。
- 检查长度是否为数字，并记录重复板号、缺失板号、缺失长度等警告。

### 2.4 衬砌平面图 DXF 输出模块

文件：

- `draw_cad/LiningPlanDxfWriter.h`
- `draw_cad/LiningPlanDxfWriter.cpp`

职责：

- 读取 `disease_report.json` 中出现过的衬砌板号。
- 读取 `board_lengths.json` 中对应板号的长度。
- 按 5 个衬砌为一块图生成平面图。
- 按板长控制每个板号占用的横向宽度。
- 绘制左侧 5 条位置标签、底部板号标签、网格线和分割线。
- 输出 `lining_plan.dxf`。

### 2.5 命令行入口

文件：

- `draw_cad/main.cpp`

职责：

- 提供命令行模式：
  - `--disease-report`
  - `--board-lengths`
  - `--lining-plan`
- 无参数运行时，默认导出病害报告 JSON 和板长 JSON。

## 3. Excel 读取模块说明

### 3.1 `InspectionRow`

`InspectionRow` 是从病害检测报告 Excel 中读取出来的一行原始业务数据。

字段说明：

- `stake_no`：桩号。
- `slab_no_raw`：衬砌板块号原始文本。
- `project_name`：项目名称。
- `defect_location_raw`：病害位置原始文本。
- `check_item_raw`：检查内容原始文本。
- `defect_desc_raw`：病害描述原始文本。
- `judgement_raw`：判定结论原始文本。
- `inspect_date_raw`：检测日期原始文本。
- `source_row`：该数据来自 Excel 的第几行，1-based。

这个结构只负责保存原始读取结果，不做业务判断。后续模块可以根据需要进一步解析。

### 3.2 `InspectionTableReader::ColumnMapping`

`ColumnMapping` 保存需要读取的 Excel 表头名称。

默认表头包括：

- `桩号`
- `衬砌板块号`
- `项目名称`
- `病害位置`
- `检查内容`
- `病害描述`
- `判定结论`
- `检测日期`

代码中使用 `u8"\u...."` 形式保存中文默认值，避免 C++ 源文件编码变化导致中文表头匹配失败。

### 3.3 `InspectionTableReader::Options`

配置项：

- `sheet_name`：工作表名称。为空时读取第一个工作表。
- `header_row`：表头所在行，默认第 1 行。
- `skip_empty_rows`：是否跳过所有字段为空的数据行，默认跳过。
- `trim_text`：是否裁剪单元格前后 ASCII 空白，默认裁剪。
- `columns`：表头名称映射。

### 3.4 表头解析逻辑

读取流程：

1. 打开 Excel 文件。
2. 选择工作表。
3. 读取 `header_row` 的所有列。
4. 将表头文本标准化：
   - 去除 UTF-8 BOM。
   - 裁剪 ASCII 空白。
5. 建立 `header_to_column` 映射。
6. 根据 `ColumnMapping` 定位所有必需列。
7. 如果缺少必需表头，返回错误。

重复表头处理：

- 如果发现重复表头，只使用第一次出现的列。
- 重复项会写入 `ReadResult::warnings`。

### 3.5 单元格转字符串逻辑

`cellToString()` 支持以下 Excel 单元格类型：

- Empty：返回空字符串。
- Boolean：返回 `true` 或 `false`。
- Integer：默认返回整数文本。
- Float：默认使用 `mytool::formatNumber()` 格式化。
- String/Error：返回原始字符串。

检测日期列会启用 `prefer_excel_date`：

- 对 Excel 日期序列号调用 `OpenXLSX::XLDateTime`。
- 仅有日期时输出 `YYYY-MM-DD`。
- 有时间时输出 `YYYY-MM-DD HH:MM:SS`。
- 日期转换失败时退回数字字符串。

## 4. 病害报告 JSON 导出说明

### 4.1 入口类

类名：

```cpp
DiseaseReportJsonExporter
```

入口函数：

```cpp
static ExportResult run(
    const std::string& input_xlsx,
    const std::string& output_json,
    const Options& options = {});
```

### 4.2 默认 Excel 列位

当前实现按固定列位读取：

- B 列：衬砌板块号。
- D 列：病害位置。
- E 列：检查内容。
- F 列：病害描述。
- G 列：判定结论。

要求工作表至少包含 A-G 列。

### 4.3 判定结论记录

函数：

```cpp
isZeroJudgement()
```

逻辑：

- 读取 G 列判定结论。
- 如果可以严格解析为数字，并且数值接近 0，则标记 `judgement_is_zero = true`。
- G 列为 0 的记录也会写入 JSON，不再跳过。
- G 列为 0 的记录数量写入 `zero_judgement_count`。

### 4.4 病害描述解析

从 F 列病害描述中解析以下标记后的文本：

- `距板端`
- `长度为`
- `宽度为`
- `面积为`

解析函数：

```cpp
extractMarkedValue()
```

截断分隔符包括：

- 中文逗号 `，`
- 英文逗号 `,`
- 中文分号 `；`
- 英文分号 `;`
- 中文句号 `。`
- 换行符

### 4.5 输出 JSON 结构

根对象包含：

```json
{
  "source": {
    "file": "...",
    "sheet": "...",
    "header_row": 1
  },
  "summary": {
    "exported_count": 0,
    "zero_judgement_count": 0
  },
  "records": []
}
```

每条记录结构：

```json
{
  "source_row": 2,
  "slab_no": "4",
  "defect_location": "...",
  "check_item": "...",
  "defect_desc": "...",
  "judgement": "0",
  "judgement_is_zero": true,
  "parsed": {
    "distance_from_slab_end": "...",
    "length": "...",
    "width": "...",
    "area": "..."
  }
}
```

## 5. 板长 JSON 导出说明

### 5.1 入口类

类名：

```cpp
BoardLengthJsonExporter
```

入口函数：

```cpp
static ExportResult run(
    const std::string& input_xlsx,
    const std::string& output_json,
    const Options& options = {});
```

### 5.2 默认读取规则

默认配置：

- `header_row = 2`
- 从第 3 行开始读取数据。
- 同时读取两组列：
  - `A:B`
  - `D:E`

列含义：

- A 列：板号。
- B 列：板长，单位 m。
- D 列：板号。
- E 列：板长，单位 m。

### 5.3 行校验逻辑

每组板号和长度会独立校验：

- 有长度但没有板号：跳过并记录 warning。
- 有板号但没有长度：跳过并记录 warning。
- 长度不是严格数字：跳过并记录 warning。
- 板号重复：导出后记录 warning。

### 5.4 排序逻辑

导出前对记录排序：

1. 数字板号优先。
2. 数字板号按数值升序。
3. 非数字板号按字符串升序。

这样可以保证后续绘图时板号顺序稳定。

### 5.5 输出 JSON 结构

根对象包含：

```json
{
  "source": {
    "file": "...",
    "sheet": "...",
    "header_row": 2,
    "length_unit": "m",
    "columns": {
      "A": "slab_no",
      "B": "length_m",
      "D": "slab_no",
      "E": "length_m"
    }
  },
  "summary": {
    "exported_count": 0
  },
  "records": []
}
```

每条记录结构：

```json
{
  "source_row": 3,
  "column_group": "A:B",
  "slab_no": "4",
  "length": "8m",
  "length_raw": "8",
  "length_m": 8.0
}
```

## 6. 衬砌平面图 DXF 输出说明

### 6.1 入口类

类名：

```cpp
LiningPlanDxfWriter
```

入口函数：

```cpp
static WriteResult run(
    const std::string& disease_report_json,
    const std::string& board_lengths_json,
    const std::string& output_dxf,
    const Options& options = {});
```

### 6.2 配置项

`LiningPlanDxfWriter::Options`：

- `slabs_per_panel = 5`：每块平面图显示 5 个衬砌。
- `drawing_units_per_meter = 1.0`：1 米对应多少绘图单位。
- `default_slab_length_m = 8.0`：缺失板长时使用的默认长度。
- `label_column_width = 10.0`：左侧文字标签列宽度。
- `zone_height = 2.0`：每条衬砌位置带高度。
- `bottom_label_height = 0.75`：底部板号行高度。
- `panel_gap_y = 2.0`：多块图之间的竖向间距。
- `text_height = 0.35`：左侧中文文字高度。
- `slab_no_text_height = 0.4`：底部板号文字高度。
- `disease_horizontal_line_clearance = 0.20`：病害图形与横向分隔线之间的最小避让距离。
- `break_line_gap = 0.18`：非连续板号断开符号中两条折线的间距。
- `break_offset_x = 0.35`：折线横向偏移量。
- `break_half_height = 0.55`：折线每个折角的竖向高度单位。

### 6.3 数据读取流程

`run()` 的主要流程：

1. 校验输入 JSON 路径和输出 DXF 路径。
2. 调用 `loadDiseaseSlabs()` 读取 `disease_report.json`。
3. 调用 `loadBoardLengths()` 读取 `board_lengths.json`。
4. 为每个板号匹配长度。
5. 缺失长度时使用 `default_slab_length_m` 并输出 warning。
6. 调用 `buildDrawing()` 构建中间绘图数据。
7. 通过 `libdxfrw` 写出 DXF。

### 6.4 病害板号读取逻辑

函数：

```cpp
loadDiseaseSlabs()
```

读取规则：

- 输入 JSON 必须包含 `records` 数组。
- 每条记录读取 `slab_no` 字符串。
- 空板号跳过。
- 数字板号会严格解析为整数。
- 重复数字板号只保留一次。
- 重复文本板号只保留一次。
- 最终按板号排序。

排序规则：

1. 数字板号优先。
2. 数字板号按整数升序。
3. 非数字板号按字符串升序。

### 6.5 板长读取逻辑

函数：

```cpp
loadBoardLengths()
```

读取规则：

- 输入 JSON 必须包含 `records` 数组。
- 每条记录读取：
  - `slab_no`
  - `length_m`
- `slab_no` 必须能严格解析为整数。
- `length_m` 必须是有限正数。
- 结果保存为 `unordered_map<int, double>`，键为数字板号。

### 6.6 图形中间结构

为了让业务绘图逻辑和 DXF 写入逻辑解耦，代码定义了中间结构：

```cpp
struct DrawLine;
struct DrawPolyline;
struct DrawText;
struct DrawingData;
```

含义：

- `DrawLine`：线段。
- `DrawPolyline`：轻量多段线。
- `DrawText`：文字。
- `DrawingData`：完整图形，包括线、折线、文字集合。

绘图阶段只负责生成这些中间图元，最终由 `LiningPlanDxfInterface` 转换成 DXF 实体。

### 6.7 单块平面图绘制逻辑

函数：

```cpp
appendPanel()
```

每块平面图包含：

- 外框。
- 左侧标签列。
- 底部板号行。
- 4 个衬砌主体区域：
  - 左侧边墙
  - 左侧拱腰
  - 右侧拱腰
  - 右侧边墙
- 5 条左侧线名：
  - 左侧边墙底线
  - 左侧拱腰线
  - 拱顶线
  - 右侧拱腰线
  - 右侧边墙底线
- `拱顶线` 对应主体区域的中间横线，即上方两个区域和下方两个区域的分界线。
- 横向分隔线。
- 板号之间的竖向分割线。
- 底部居中的板号文本。

板号显示格式：

- 数字板号在 0 到 999 范围内时，显示为 3 位，例如 `4` 显示为 `004`。
- 非数字板号保留原文本。

### 6.8 多块平面图排列逻辑

函数：

```cpp
buildDrawing()
```

逻辑：

- 每 `slabs_per_panel` 个板号生成一块图。
- 默认每块 5 个板号。
- 多块图沿 Y 方向向下排列。
- 块之间间距为 `panel_gap_y`。

例如 18 个板号时：

- 第 1 块：5 个板号。
- 第 2 块：5 个板号。
- 第 3 块：5 个板号。
- 第 4 块：3 个板号。

## 7. 分割线规则说明

### 7.1 相邻板号

函数：

```cpp
areAdjacent()
```

判断条件：

- 左右两个板号都能解析为数字。
- 右侧板号 = 左侧板号 + 1。

满足条件时，使用一条完整竖线从底部到顶部贯通。

### 7.2 不相邻板号

不满足相邻条件时，表示中间存在未绘出的板号区间。

绘图规则：

- 底部板号行仍使用直线分割。
- 图形主体区域使用断开折线符号。
- 折线实体位于 `BREAK` 图层。

### 7.3 当前折线形状

函数：

```cpp
addBreakLine()
```

当前实现为两条并行折线，每条折线有 7 个顶点：

```text
上方竖线
向右折出
回到中线
向左折出
回到中线
下方竖线
```

实际顶点逻辑：

```cpp
{ line_x, y_top },
{ line_x, mid_y + half_height * 2.0 },
{ line_x + offset_x, mid_y + half_height },
{ line_x, mid_y },
{ line_x - offset_x, mid_y - half_height },
{ line_x, mid_y - half_height * 2.0 },
{ line_x, y_bottom }
```

这样形成上下两个方向相反的折角，符合当前参考图。

两条并行折线的位置：

```cpp
line_x = x - break_line_gap / 2
line_x = x + break_line_gap / 2
```

## 8. DXF 中文显示处理

### 8.1 问题背景

最初直接把 UTF-8 中文字符串写入 DXF `TEXT` 实体，部分 CAD 查看器不会按 UTF-8 正确解释，导致中文不显示。

### 8.2 解决方案

当前采用两层处理：

1. 写入中文文字样式 `CN_TEXT`。
2. 将文字内容转换为 DXF Unicode 转义形式。

文字样式：

```cpp
constexpr const char* kChineseTextStyleName = "CN_TEXT";
constexpr const char* kChineseFontFileName = "simsun.ttc";
```

写入方式：

```cpp
DRW_Textstyle text_style;
text_style.name = kChineseTextStyleName;
text_style.font = kChineseFontFileName;
writer_.writeTextstyle(&text_style);
```

文字实体使用：

```cpp
text.style = kChineseTextStyleName;
text.text = toDxfText(item.text);
```

### 8.3 Unicode 转义函数

函数：

```cpp
toDxfText()
```

逻辑：

- 逐个读取 UTF-8 codepoint。
- ASCII 可见字符直接保留。
- 非 ASCII 字符转换为 `\U+XXXX`。
- 超出 BMP 的字符转换为 UTF-16 surrogate pair 对应的两个 `\U+XXXX`。

示例：

```text
左侧边墙底线
```

会写入类似：

```text
\U+5DE6\U+4FA7\U+8FB9\U+5899\U+5E95\U+7EBF
```

## 8.4 病害符号映射优先级

病害符号映射以 `check_item` 为主，不再让 `defect_desc` 中的伴随描述覆盖主病害类型。

映射规则：

- 先读取 `check_item` 并匹配主病害类型。
- 如果 `check_item` 能匹配到符号，直接使用该符号。
- 只有 `check_item` 无法识别时，才使用 `defect_desc` 兜底匹配。
- 在 `defect_desc` 兜底匹配时，裂缝、露筋、破损等主病害优先于“渗水印迹”。
- 例如 `check_item` 为 `纵向裂缝`，但 `defect_desc` 包含 `伴有渗水印迹` 时，仍映射为纵向裂缝符号，不映射为渗水印迹符号。

## 8.5 病害符号缩放规则

病害符号缩放和文字标注分开处理：

- `纵向裂缝` 按 `length` 横向缩放。
- `环向裂缝` 和 `横向裂缝` 按 `length` 纵向缩放。
- `斜向裂缝` 不按 `length` 缩放，始终使用默认符号比例。
- `斜向裂缝` 仍可显示 `L=...` 长度文字，只是不改变图形尺寸。

## 8.6 病害重叠避让

病害绘制阶段会为每个病害计算一个占用矩形，范围包含：

- 病害符号本身。
- 渗水印迹内部面积文字。
- 其它病害旁边的文字标注。

绘制每个病害前，会把当前占用矩形与已经放置的病害矩形逐个比较。如果发现重叠，程序会在当前衬砌板块范围内按小步长尝试偏移位置：

- 优先保持原始位置。
- 其次尝试向上、向下偏移。
- 再尝试向左、向右偏移。
- 纵向避让必须限制在 `defect_location` 对应的部位带内，例如 `左拱腰` 的病害只能在左拱腰区域内调整，不能跨过拱顶线进入右拱腰区域。
- 病害图形会优先放在方框内部安全区域，避开横向分隔线。
- `拱顶` 病害限制在拱顶线 `-0.2m` 到 `+0.2m` 范围内，并优先靠近拱顶线上侧的边界。
- 文字描述允许压在横向分隔线上，不作为病害图形避让横线的约束。
- 当符号高度能放入对应部位带时，符号上下边界也会被限制在该部位带内。
- 每次候选位置都会计算与已放置病害的重叠面积，以及病害图形越出所属部位带的惩罚分。
- 如果找到完全不重叠的位置，立即使用该位置。
- 如果空间不足无法完全避开，则选择重叠面积最小的位置。

相关参数位于 `LiningPlanDxfWriter::Options`：

- `disease_collision_padding`：病害占用矩形的额外安全边距。
- `disease_collision_step_x`：横向避让步长。
- `disease_collision_step_y`：纵向避让步长。
- `disease_collision_max_offset_steps`：最大偏移搜索步数。
- `disease_horizontal_line_clearance`：病害图形避开横向分隔线的距离。

该逻辑只改变 DXF 中病害符号的显示位置，不修改 `disease_report.json` 中的原始病害数据。

## 9. DXF 图层说明

衬砌平面图使用 4 个图层：

- `FRAME`：外框、横线、普通竖向分割线。
- `BREAK`：非连续板号之间的断开折线符号。
- `DISEASE`：病害符号线段和多段线。
- `TEXT`：左侧中文标签和底部板号文字。

图层在 `LiningPlanDxfInterface::writeLayers()` 中写入。

## 10. 命令行使用方式

可执行文件路径：

```powershell
d:\vs2022 code\auto_cad\x64\Debug\draw_cad.exe
```

### 10.1 导出病害报告 JSON

```powershell
& 'd:\vs2022 code\auto_cad\x64\Debug\draw_cad.exe' --disease-report 'd:\vs2022 code\auto_cad\病害检测报告.xlsx' 'd:\vs2022 code\auto_cad\disease_report.json'
```

不传路径时使用默认路径：

- 输入：`D:\vs2022 code\auto_cad\病害检测报告.xlsx`
- 输出：`D:\vs2022 code\auto_cad\disease_report.json`

### 10.2 导出板长 JSON

```powershell
& 'd:\vs2022 code\auto_cad\x64\Debug\draw_cad.exe' --board-lengths 'd:\vs2022 code\auto_cad\板长分布表.xlsx' 'd:\vs2022 code\auto_cad\board_lengths.json'
```

不传路径时使用默认路径：

- 输入：`D:\vs2022 code\auto_cad\板长分布表.xlsx`
- 输出：`D:\vs2022 code\auto_cad\board_lengths.json`

### 10.3 生成衬砌平面图 DXF

```powershell
& 'd:\vs2022 code\auto_cad\x64\Debug\draw_cad.exe' --lining-plan 'd:\vs2022 code\auto_cad\disease_report.json' 'd:\vs2022 code\auto_cad\board_lengths.json' 'd:\vs2022 code\auto_cad\lining_plan.dxf'
```

不传路径时使用默认路径：

- 输入病害 JSON：`D:\vs2022 code\auto_cad\disease_report.json`
- 输入板长 JSON：`D:\vs2022 code\auto_cad\board_lengths.json`
- 输出 DXF：`D:\vs2022 code\auto_cad\lining_plan.dxf`

### 10.4 无参数运行

无参数运行时会依次执行：

1. 导出 `disease_report.json`。
2. 导出 `board_lengths.json`。

不会自动生成 `lining_plan.dxf`，生成 DXF 需要显式使用 `--lining-plan`。

## 11. 错误和 warning 处理

### 11.1 `InspectionTableReader::ReadResult`

字段：

- `rows`：成功读取的数据行。
- `warnings`：非致命问题。
- `errors`：致命错误。

`ok()` 判断：

```cpp
return errors.empty();
```

### 11.2 JSON 导出模块 `ExportResult`

字段：

- `code`：错误码，0 表示成功。
- `message`：结果信息。
- `exported_count`：导出数量。
- `warnings`：非致命问题。

病害报告导出额外包含：

- `zero_judgement_count`：G 列判定结论为 0 的记录数量。这些记录仍会写入 JSON，并参与 DXF 病害绘制。

### 11.3 DXF 输出模块 `WriteResult`

字段：

- `code`：错误码，0 表示成功。
- `message`：结果信息。
- `slab_count`：绘制的唯一板号数量。
- `panel_count`：生成的平面图块数。
- `warnings`：非致命问题。

常见 warning：

- 某个病害板号在 `board_lengths.json` 中没有找到板长。
- 程序会使用默认板长继续绘图。

## 12. 构建和验证记录

已使用 Visual Studio 2022 MSBuild 构建 Debug x64。

构建命令：

```powershell
$pathValue = $env:Path
[Environment]::SetEnvironmentVariable('PATH', $null, 'Process')
[Environment]::SetEnvironmentVariable('Path', $pathValue, 'Process')
& 'd:\vs2022\Microsoft Visual Studio\2022\Community\MSBuild\Current\Bin\MSBuild.exe' 'draw_cad.sln' /t:Build /p:Configuration=Debug /p:Platform=x64 /m
```

说明：

- 当前 PowerShell 进程中可能同时存在 `Path` 和 `PATH`，MSBuild 启动 `CL.exe` 时会报重复键错误。
- 上述命令先清理当前进程里的重复环境变量，再调用 MSBuild。

已验证：

- Debug x64 构建通过。
- 已生成 `lining_plan.dxf`。
- 当前数据生成结果为：
  - `slabs: 36`
  - `panels: 8`
- DXF 中包含：
  - `CN_TEXT`
  - `simsun.ttc`
  - 中文 `\U+XXXX` 转义
  - `BREAK` 图层
  - 7 顶点折线断开符号

仍存在的非阻塞 warning：

- `libxfrw/drw_entities.h` 中有 `C4267` 类型转换 warning。
- `OpenXLSXd.lib` 可能缺少调试 PDB warning。

这些 warning 属于第三方库或既有依赖，不影响本次功能运行。

## 13. 后续扩展建议

当前代码已经能完成从 Excel/JSON 到衬砌平面图 DXF 的基本链路。后续如果继续扩展，可以优先考虑：

- 将病害报告导出模块改为复用 `InspectionTableReader` 的表头定位能力，减少固定列位依赖。
- 将 `LiningPlanDxfWriter::Options` 暴露到命令行，方便调整图纸比例、文字大小、折线尺寸。
- 为 DXF 输出增加图纸标题、比例尺、里程范围等信息。
- 将缺失板长 warning 输出到单独日志文件，方便人工补齐数据。
- 增加 DXF 输出单元测试，验证板号排序、断开符号顶点、中文转义和图层写入。
